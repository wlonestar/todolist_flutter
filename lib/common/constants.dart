import 'package:flutter/material.dart';

const appName = 'Todo List';
const setting = 'Settings';
const privacy = 'Privacy';

const sendRequiredData =
    'We gather diagnostic data required to keep To Do secure, up-to-date and performing as expected on the devices it\'s installed on. For example, what operating system you\'re using, and if updates are installed successfully.';
const sendOptionalData =
    'We\'d like additional diagnostic and usage data sent to us. It\'s how we continue to improve. None of this data includes your name, file contents, or information about apps unrelated to To Do.';

const version = 'Version';

const Color cardColor = Colors.white;
const Color checkColor = Colors.black45;
